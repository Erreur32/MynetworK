# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies & build
node_modules
dist
dist-ssr
*.local

# AI tools & dev assistants
CLAUDE.md
.claude/
.claude.*
.cursorrules
.cursorignore
.cursor/
.copilot*
.gemini/
GEMINI.md
AGENTS.md
.planning/
.superpowers/
docs/superpowers/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.code-workspace
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment variables (sensitive - contains secrets, tokens, API keys)
.env
.env.*

# Build artifacts
commit-message.txt

# Database files (contains sensitive data: users, passwords, tokens, configs)
data/
*.db
*.db-shm
*.db-wal

# Configuration files (contains sensitive data: passwords, tokens, API keys)
config/mynetwork.conf
config/*.conf
!config/*.example
!config/*.example.conf

# Private development folders (tokens, internal docs, secrets)
Doc_dev/
Doc_Dev/
Aide*
MyNetwork_old_good

# Scripts with hardcoded credentials (development only)
test-backend.sh
unifi.sh
unifi_script.sh
unifi_token.sh
unifi_token_interactif.sh
unitfi_test.sh

# Backup files
*.backup
*.bak
*~

# Secrets and sensitive files
*secret*
# Token credential files (specific patterns to avoid excluding source code)
*.token
*.token.*
*_token
*_token.*
.freebox_token*
**/token.json
**/token.txt
*.key
*.pem
*.p12
*.pfx
*.jks
*.keystore
*.crt
*.cert
*.pwd
*.passwd
*.password

# OS files
Thumbs.db
.DS_Store
.AppleDouble
.LSOverride
._*

# MemPalace local tooling (not shared across contributors)
mempalace.yaml
entities.json
