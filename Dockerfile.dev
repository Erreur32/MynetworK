# ===========================================
# MynetworK - Multi-Source Network Dashboard
# Development Dockerfile - Hot reload enabled
# ===========================================

FROM node:20-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm ci

# Source code will be mounted via volume
# No COPY of source code here

# Vite dev server + API server ports
# Frontend (Vite): 5173 or VITE_PORT (default 3000 in docker-compose)
# Backend (API): 3003 (SERVER_PORT)
EXPOSE 5173 3003

# Start dev server with hot reload
CMD ["npm", "run", "dev"]
